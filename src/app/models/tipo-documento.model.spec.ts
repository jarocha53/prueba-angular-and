import { TipoDocumentoModel } from './tipo-documento.model';

describe('TipoDocumento.Model', () => {
  it('should create an instance', () => {
    expect(new TipoDocumentoModel()).toBeTruthy();
  });
});
