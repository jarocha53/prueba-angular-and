import { TipoDocumento.Service } from './tipo-documento.service';

describe('TipoDocumento.Service', () => {
  it('should create an instance', () => {
    expect(new TipoDocumento.Service()).toBeTruthy();
  });
});
